
```
1. 헤이즐 캐스트 분산 맵에 들어가는 데이터는 직렬화가 가능해야함
   - 네트워크 통신이 필요하므로
2. 헤이즐 캐스트 분산 Map 에 들어가는 클래스는 Serializable 구현 필요
```


![[Pasted image 20251209154940.png]]

예외가 발생하고 docs 에 따르면 `Serializable` 인터페이스를 구현하지 않으면 발생한다. 다시 말해 `Serializable` 구현을 강제하고 있다.
```
Thrown when an instance is required to have a Serializable interface. The serialization runtime or the class of the instance can throw this exception. The argument should be the name of the class.
```

일반적으로 우리가 만든 객체가 아니라 String, Integer 같이 `Serializable` 를 구현한 래퍼 타입을 캐시하기 때문에 발견하지 못했다.